name: blank

on:
  push:

jobs:
  highway:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        draught: ["confront", "ensure", "youngster", "lifetime", "lick"]
        appliance: ["incredible", "brow", "anxiety", "storey"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Yr
